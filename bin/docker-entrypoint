#!/bin/bash -e

# TO RESTORE DB: uncomment the block below to create/migrate the database on boot
# if [ "${@: -2:1}" == "./bin/rails" ] && [ "${@: -1:1}" == "server" ]; then
#   ./bin/rails db:prepare
# fi

exec "${@}"
